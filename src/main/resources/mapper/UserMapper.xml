<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.board.service.user.UserMapper">
    <select id="findByUsername" resultType="UserVO">
        select * from user where login_id = #{username}
    </select>

    <insert id="join" parameterType="JoinReqDTO">
        insert into user(login_id, password, name, nickname, birth, phone, email, provider, provider_id, role)
        values(#{username}, #{password}, #{name}, #{nickname}, #{birth}, #{phone}, #{email}, #{provider}, #{provider_id}, #{role})
    </insert>
</mapper>