<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.board.service.comment.CommentMapper">
    <insert id="registComment" parameterType="CommentRegistReqDto">
        insert into comment(content, member_id, board_id)
        values(#{content}, #{memberId}, #{boardId})

        <selectKey keyProperty="id" resultType="long" order="AFTER">
            select last_insert_id()
        </selectKey>
    </insert>

    <select id="findById" resultType="CommentRegistResDto">
        select content, reg_date, mod_date, is_deleted, member_id, parent_com_id
        from comment
        where id = #{id}
    </select>

    <select id="getCommentList" resultType="CommentRegistResDto">
        select c.id, c.content, c.reg_date, c.mod_date, c.is_deleted, c.member_id, c.parent_com_id, u.nickname
        from comment c
        left join user u on c.member_id = u.id
        where board_id = #{boardId}
    </select>

    <update id="deleteComment" parameterType="CommentDeleteReqDto">
        update comment set is_deleted = true
        where id = #{id}
    </update>
</mapper>